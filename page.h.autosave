#ifndef PAGE_H
#define PAGE_H

#include <QObject>

class Page : public QObject
{
    Q_OBJECT

public:
    Page(QString name = 0, int index = 0, bool pMultibloc = false, bool pOrdonable = false);

    //Getters
    QString getPageName();
    int getPageIndex();
    bool isMultiblock();
    bool isOrdonable();

protected:
    QString pageName;
    int pageIndex;
    bool multiBlock;
    bool ordonable;
};

#endif // PAGE_H
